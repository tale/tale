# Python aliases so I don't need to put a '3'
alias python=/usr/local/bin/python3
alias pip=/usr/local/bin/pip3

# Theos stuff so that I don't suffer
export THEOS=~/.theos
alias i7="THEOS_DEVICE_IP=192.168.1.230"
alias i6="THEOS_DEVICE_IP=192.168.1.249"
alias final="FINALPACKAGE=1 DEBUG=0"
